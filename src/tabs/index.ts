/**
 * Tabs component system
 *
 * Provides tabbed interface components for organizing content
 * into multiple panes where only one pane is visible at a time.
 */

export { TabController } from './TabController.ts';
export { Tab } from './Tab.ts';
export { Pane } from './Pane.ts';
export type { TabOptions } from './types.ts';
